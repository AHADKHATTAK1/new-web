

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="testimonials color-{{ section.settings.color_scheme }} gradient">
  <div class="page-width section-{{ section.id }}-padding">
    
    {%- unless section.settings.heading == blank -%}
      <div class="title-wrapper text-center" style="margin-bottom: 3rem;">
        <h2 class="title {{ section.settings.heading_size }}">{{ section.settings.heading | escape }}</h2}
        {%- unless section.settings.subheading == blank -%}
          <p style="color: rgba(var(--color-foreground-rgb), 0.7); margin-top: 0.5rem; font-size: 1.1rem;">
            {{ section.settings.subheading }}
          </p>
        {%- endunless -%}
      </div>
    {%- endunless -%}
    
    <div class="testimonials-grid grid grid--1-col grid--2-col-tablet grid--{{ section.settings.columns_desktop }}-col-desktop" style="gap: 2rem;">
      {%- for block in section.blocks -%}
        {%- if block.type == 'testimonial' -%}
          <div class="testimonial-card" {{ block.shopify_attributes }} style="background: rgba(var(--color-foreground-rgb), 0.03); padding: 2rem; border-radius: 12px; position: relative; transition: transform 0.3s, box-shadow 0.3s;">
            
            <!-- Quote Icon -->
            <div style="position: absolute; top: 1rem; right: 1rem; font-size: 3rem; opacity: 0.1;">
              "
            </div>
            
            <!-- Rating Stars -->
            {%- if block.settings.rating > 0 -%}
              <div class="testimonial-rating" style="display: flex; gap: 4px; margin-bottom: 1rem;">
                {%- for i in (1..5) -%}
                  {%- if i <= block.settings.rating -%}
                    <span style="color: #ffc107; font-size: 1.2rem;">★</span>
                  {%- else -%}
                    <span style="color: #ddd; font-size: 1.2rem;">★</span>
                  {%- endif -%}
                {%- endfor -%}
              </div>
            {%- endif -%}
            
            <!-- Testimonial Text -->
            {%- unless block.settings.text == blank -%}
              <div class="testimonial-text" style="font-size: 1rem; line-height: 1.6; color: rgba(var(--color-foreground-rgb), 0.8); margin-bottom: 1.5rem; font-style: italic;">
                "{{ block.settings.text }}"
              </div>
            {%- endunless -%}
            
            <!-- Author Info -->
            <div class="testimonial-author" style="display: flex; align-items: center; gap: 1rem;">
              {%- if block.settings.image != blank -%}
                {{
                  block.settings.image
                  | image_url: width: 120
                  | image_tag: loading: 'lazy', class: 'testimonial-avatar', style: 'width: 50px; height: 50px; border-radius: 50%; object-fit: cover;'
                }}
              {%- else -%}
                <div style="width: 50px; height: 50px; border-radius: 50%; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center; color: white; font-weight: 700; font-size: 1.2rem;">
                  {{ block.settings.author_name | slice: 0 }}
                </div>
              {%- endif -%}
              
              <div>
                {%- unless block.settings.author_name == blank -%}
                  <div style="font-weight: 600; font-size: 0.95rem;">
                    {{ block.settings.author_name }}
                  </div>
                {%- endunless -%}
                {%- unless block.settings.author_title == blank -%}
                  <div style="font-size: 0.85rem; color: rgba(var(--color-foreground-rgb), 0.6);">
                    {{ block.settings.author_title }}
                  </div>
                {%- endunless -%}
              </div>
            </div>
            
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
    
  </div>
</div>

<style>
  .testimonial-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(0,0,0,0.15);
  }
</style>

{% schema %}
{
  "name": "Testimonials",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "What Our Customers Say"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Real reviews from real customers"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        {
          "value": "h2",
          "label": "Small"
        },
        {
          "value": "h1",
          "label": "Medium"
        },
        {
          "value": "h0",
          "label": "Large"
        }
      ],
      "default": "h1"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 4,
      "step": 1,
      "label": "Number of columns on desktop",
      "default": 3
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "textarea",
          "id": "text",
          "label": "Testimonial text",
          "default": "This product exceeded my expectations! Great quality and fast shipping."
        },
        {
          "type": "range",
          "id": "rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "label": "Rating (stars)",
          "default": 5
        },
        {
          "type": "text",
          "id": "author_name",
          "label": "Author name",
          "default": "John Doe"
        },
        {
          "type": "text",
          "id": "author_title",
          "label": "Author title/location",
          "default": "Verified Buyer"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Author image (optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "text": "Amazing quality! This product exceeded all my expectations. Highly recommended!",
            "rating": 5,
            "author_name": "Sarah Johnson",
            "author_title": "Verified Buyer"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "text": "Fast shipping and excellent customer service. Will definitely buy again!",
            "rating": 5,
            "author_name": "Mike Chen",
            "author_title": "Verified Buyer"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "text": "Best purchase I've made this year. The quality is top-notch!",
            "rating": 5,
            "author_name": "Emma Davis",
            "author_title": "Verified Buyer"
          }
        }
      ]
    }
  ]
}
{% endschema %}
